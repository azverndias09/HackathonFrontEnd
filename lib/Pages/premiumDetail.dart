import 'package:flutter/material.dart';

class PremiumDetailPage extends StatelessWidget {
  const PremiumDetailPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Premium Details"),
      ),
    );
  }
}
